anthropic
fastapi
uvicorn[standard]
langchain
langsmith>=0.4,<1
langgraph
# CrewAI 1.15 aligns with Pydantic 2.12 on the Python 3.12 runtime used by
# Docker and CI. Keep this floor explicit so Python 3.11 cannot backtrack to
# CrewAI 1.14's incompatible Pydantic 2.11 constraint.
crewai>=1.15.2,<2
qdrant-client
pypdf
pypdfium2>=4.30,<5
scikit-learn
pandas
numpy
pydantic-settings
python-dotenv
websockets
sqlalchemy[asyncio]>=2.0
aiosqlite
asyncpg
python-multipart
httpx
cryptography
beautifulsoup4
# The slim distribution still provides the `pydantic_ai` import surface while
# avoiding provider SDKs that Govern.ai never uses.
pydantic-ai-slim[anthropic,openai]==1.104.0
pyjwt
bcrypt
authlib
itsdangerous
email-validator
pytest
pytest-asyncio
prometheus-client
pyyaml
ruff
black

# Optional: NER-based PII redaction (PII_ENGINE=presidio). Heavy (pulls spaCy);
# the default regex engine needs none of this. To enable:
#   pip install presidio-analyzer presidio-anonymizer && python -m spacy download en_core_web_lg
# presidio-analyzer
# presidio-anonymizer
